class Карта {
    field string масть;
    field string строковоеПредставление;
    field int значение;

    constructor Карта new(string масть, string строковоеПредставление, int значение) {
        let масть = масть;
        let строковоеПредставление = строковоеПредставление;
        let значение = значение;

        return this;
    }
}