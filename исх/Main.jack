class Main {
    function void main() {
        var int семя;
        var int количествоИгроков;

        var Игра игра;
        
        let семя = Keyboard.readInt("Vvedite semya: ");
        do Случайность.УстановитьСемя(семя);

        let количествоИгроков = Keyboard.readInt("Vvedite koli4estvo igrokov: ");

        do Экран.Очистить();

        let игра = Игра.Новый(количествоИгроков);

        do игра.Стартовать();

        return;
    }
}
